import { BlurEventDirective } from './blur-event.directive';

describe('BlurEventDirective', () => {
  it('should create an instance', () => {
    const directive = new BlurEventDirective();
    expect(directive).toBeTruthy();
  });
});
